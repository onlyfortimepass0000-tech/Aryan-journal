# Shared secret that gates the whole app (UI + API). Set this in Vercel project settings.
ACCESS_TOKEN=

# Provided automatically when you add Vercel Postgres to the project.
# Only needed locally if you want to run against a real database.
POSTGRES_URL=
