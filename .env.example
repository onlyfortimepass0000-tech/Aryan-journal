# Shared secret that gates the whole app (UI + API). Set this in Vercel project settings.
ACCESS_TOKEN=

# Supabase Postgres, accessed over its REST API (PostgREST) — no client SDK.
SUPABASE_URL=
SUPABASE_ANON_KEY=
